bash_init()
{
	local paths="/etc/bashrc,~/.bash,~/bin/bashrc${1:+,$1}"
	local files="global,os.$(uname),host.$HOSTNAME"
	local search=( `eval echo {${paths}}/{${files}}` )
	local f
	for f in ${search[@]}; do
		[ -f "$f" ] && . "$f" # 2>/dev/null
	done
}
bash_init "$@"
unset -f bash_init
